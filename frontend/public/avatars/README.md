# Avatars

- `default.png` — placeholder shown until the user uploads their own avatar.

User uploads will be stored elsewhere at implementation time; only the default belongs in this folder for now.
